{
  "name": "skeleton",
  "private": true,
  "description": "A skeleton for basic applications and tests using Web Components",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "connect-history-api-fallback": "^1.6.0",
    "rollup": "^1.24.0",
    "rollup-plugin-browsersync": "^1.0.0"
  }
}
